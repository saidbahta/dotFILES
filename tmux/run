# initialitation session and window 
new  -s GENERAL -n MOCP mocp ~/.Download/
neww -n TERMINAL
neww -n RANGER ranger 

new  -s EDITOR -n "VIM[]" vim
neww -n "VIM{}" vim -O2
neww -n "VIM()"
split-window -v -p 25 -t 0
neww -n TERMINAL
